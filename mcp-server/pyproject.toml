[project]
name = "python-mcp"
version = "0.1.0"
description = "Job-search tool exposed over MCP 2.0 Streamable HTTP"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=2.0.0",
]
